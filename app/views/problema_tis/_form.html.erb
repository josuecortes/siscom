<%= form_with(model: problema_ti, local: true) do |form| %>
  <% if problema_ti.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(problema_ti.errors.count, "error") %> prohibited this problema_ti from being saved:</h2>

      <ul>
        <% problema_ti.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome %>
  </div>

  <div class="field">
    <%= form.label :descricao %>
    <%= form.text_area :descricao %>
  </div>

  <div class="field">
    <%= form.label :tipo_problema_ti_id %>
    <%= form.text_field :tipo_problema_ti_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
