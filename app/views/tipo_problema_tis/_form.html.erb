<div class="modal-body">

  <%= form_for @tipo_problema_ti, remote: true do |f| %>
    
    <% if @tipo_problema_ti.errors.any? %>
      <div class="alert alert-danger alert-dismissible">
        <button type="button" class="close" data-dismiss="alert" aria-hidden="true">×</button>
        <h5><i class="icon fas fa-ban"></i> Ops! Algo deu errado.</h5>
      </div>
    <% end %>

    <div class="form-group">
      <%= f.label :nome %>
      <%= f.text_field :nome, autofocus: true, autocomplete: "Nome", placeholder: "Digite o nome", class: "form-control", required: true %>
      <%= has_error(@tipo_problema_ti, :nome) %>
    </div>

    <div class="form-group">
      <%= f.label :descricao %>
      <%= f.text_field :descricao, autocomplete: "Sigla", placeholder: "Digite a descrição", class: "form-control", required: true %>
      <%= has_error(@tipo_problema_ti, :descricao) %>
    </div>

    <div class="modal-footer">
      <button type="submit" class="btn btn-success" data-disable-with="Salvar"><i class="icon fa fa-check"></i> Salvar</button>
      <button type="button" class="btn btn-danger" data-dismiss="modal"><i class="icon fas fa-ban"></i> Cancelar</button>
    </div>

  <% end %>    

</div>




